terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 4.16"
    }
  }
  required_version = ">= 1.2.0"
}

// Specifies provider parameters
provider "aws" {
  region = var.var_aws_region
}
